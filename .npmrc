# Pinned to the public registry on purpose: this is a public deliverable and
# must install for anyone who clones it, regardless of the developer's global
# npm config (a private mirror was active during development).
registry=https://registry.npmjs.org/

engine-strict=true
